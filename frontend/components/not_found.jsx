import React from 'react';
import { Link } from 'react-router-dom';

const NotFound = (props) => {
    return (
        <div className="brokenReact" >
        </div>
    )
};

export default NotFound;